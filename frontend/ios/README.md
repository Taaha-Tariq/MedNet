# iOS Module

- Xcode projects managed by Flutter (`Runner.xcodeproj`, `Runner.xcworkspace`).
- Key configs in `Runner/Info.plist` and `Flutter/*.xcconfig`.

Build via Flutter: `flutter build ios` or `flutter run` (requires Xcode + CocoaPods).
