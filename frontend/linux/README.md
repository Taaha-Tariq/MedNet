# Linux Module

- Desktop runner with CMake.
- Source in `runner/` and generated plugin registrants in `flutter/`.

Build via Flutter: `flutter run -d linux` (requires Flutter desktop support enabled).
